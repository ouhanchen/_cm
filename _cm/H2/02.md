程式碼要點說明判別式與公式：我們直接帶入公式 $x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$。cmath.sqrt()：當輸入值為負數時，一般的 math.sqrt() 會報錯（ValueError），但 cmath.sqrt() 會自動回傳複數（例如 cmath.sqrt(-1) 會得到 1j）。浮點數精度：在電腦運算中，由於浮點數誤差，f(x) 計算出來的結果往往不是精確的 $0$（例如可能是 $1.2 \times 10^{-16}$），因此必須使用 cmath.isclose() 來判斷兩者是否「足夠接近」。